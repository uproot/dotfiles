// Stub for the never-committed shapes/shapes/corner-rounding.js.
.pragma library

function CornerRounding(radius, smoothness) {
    this.radius = radius || 0;
    this.smoothness = smoothness || 0;
}
